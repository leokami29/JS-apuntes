// Objeto Math

let resultado;

resultado = Math.PI;/* 3,1416 */
resultado = Math.round(2.5);/* redondear el numero */
resultado = Math.ceil(2.1); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); // Floor siempre redondea hacia abajo
resultado = Math.sqrt(144);/* raiz cuadrada */
resultado = Math.abs(-200);//convierte el numero en positivo
resultado = Math.min( 3, 5, 1, 8 , 2, 10 );//el numero menor de un listado
resultado = Math.max( 3, 5, 1, 8 , 2, 10 );//el numero mas alto del listado
resultado = Math.random();//genera un numero random
resultado = Math.floor( Math.random() * 30 );

console.log(resultado);